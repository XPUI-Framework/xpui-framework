[package]
name = "xpui"
description = "A declarative UI framework for e-ink firmware"
readme = "README.md"
keywords = ["ui", "embedded", "no-std", "eink", "gui"]
categories = ["embedded", "gui", "no-std"]
version = "0.1.0"
edition = "2024"
license = "MIT"
authors = ["Thiago Holanda <unnamedd@gmail.com>"]
repository = "https://github.com/XPUI-Framework/xpui-framework"

[lints]
workspace = true

# rlib only: a backend and an application produce whatever the target links —
# a staticlib for a C++ firmware, a binary for a host. A staticlib here is a
# 17 MB archive that contributes nothing to that link.
[lib]
crate-type = ["rlib"]

[features]
# Exposes the deterministic test doubles in `testing` to downstream crates, so
# a backend or an application can unit-test layouts without a renderer.
testing = []

[dependencies]

[workspace]
# `xtask` is the gate. A member rather than a separate workspace so one
# `cargo test` reaches its tests.
members = ["xtask"]

[workspace.lints.clippy]
undocumented_unsafe_blocks = "deny"
too_long_first_doc_paragraph = "deny"

[workspace.lints.rustdoc]
unescaped_backticks = "deny"

# What a `cargo build --release` of this repository alone produces — not what
# ships. A library's profile applies only at the top of a build; a firmware's
# workspace sets the profile for everything it links; `xpui-rp2040`,
# `xpui-esp32` and `xpui-cpp` use `opt-level = "z"`.
[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
